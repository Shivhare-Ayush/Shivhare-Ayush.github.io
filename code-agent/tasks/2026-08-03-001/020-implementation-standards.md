# Implementation Standards

- Asset replacement: Replace `src/assets/Ayush.png` in PNG format.
- Component update: Clean Tailwind utility class usage in `Sidebar.jsx`.
- Validation: `npm run build && npm run lint` must pass with zero errors.
