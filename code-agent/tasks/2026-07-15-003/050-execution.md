# Execution Log

## Task 1 — Card.jsx ✅
- Replaced BsArrowUpRight/BsArrowDownLeft with BsChevronDown
- Counter-clockwise rotation: `rotate-0` → `-rotate-180` on expand
- Added image guard: `{image && <img ...>}`
- Build + lint: zero errors

## Task 2 — ArticleCard.jsx ✅
- Replaced BsArrowUpRight/BsArrowDownLeft with BsChevronDown
- Counter-clockwise rotation: `rotate-0` → `-rotate-180` on expand
- Build + lint: zero errors
