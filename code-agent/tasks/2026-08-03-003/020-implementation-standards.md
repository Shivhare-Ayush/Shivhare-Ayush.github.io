# Implementation Standards

- Theme preset defined in `src/theme/themeConfig.js`
- Preset fields: `id`, `label`, `description`, `swatch`, `keyframes`
- Validation gate: `npm run build && npm run lint` must pass with zero errors
