# Specification: Dynamic Name Gradient & Default Theme

- Theme config: `DEFAULT_PRESET` set to `crimsonUltrasonic`.
- Components: `Sidebar.jsx` and `Header.jsx` compute `nameGradientStyle` from `activePreset.swatch`.
